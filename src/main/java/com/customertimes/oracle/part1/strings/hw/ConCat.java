package com.customertimes.oracle.part1.strings.hw;

/*
Given two strings,
append them together (known as "concatenation") and return the result.
However, if the concatenation creates a double-char,
then omit one of the chars, so "abc" and "cat" yields "abcat".

conCat("abc", "cat") → "abcat"
conCat("dog", "cat") → "dogcat"
conCat("abc", "") → "abc"
 */
public class ConCat {
    public static String catCon(String a, String b) {
        if (a.length() == 0 || a.length() == 1) {
            String ax = a
        }
        if (b.length() == 0 || a.length() == 1) {
            String bx = b
        }






        if (a.length() >=2 && b.length() <=2)

        if (a.length() >= 2 && b.length() >=2) {

        }












        //if ((a.length() > 1) && (b.length() > 1) && (a.substring(a.length() - 1)).equals((b.substring(0, 2)))) {
            //return a + b.substring(1);
        //return a + b;

        return "0";
    }

    public static void main(String[] args) {
        System.out.println(catCon("abc", "kbculhlhlh"));
    }
}